{
    // folder_structure defines the folder structure to store notes
    // Available format strings are listed here: https://docs.python.org/3.6/library/datetime.html#strftime-strptime-behavior
    "folder_structure": "~/DailyOrganizer/%Y/%m/%d",
    // Name of the main notes file stored in each folder
    // Extensions other than txt can also be used and will automatically
    // use the right sytax highlighter if installed (ex. Today.md for Markdown)
    "main_notes_file_name": "Today.txt",
}
